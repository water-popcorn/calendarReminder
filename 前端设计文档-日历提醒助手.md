# 日历提醒助手前端设计文档

**文档版本**：V1.0（前端实施稿）  
**依据**：[产品需求文档-日历提醒助手.md](./产品需求文档-日历提醒助手.md)、[概要设计文档-日历提醒助手.md](./概要设计文档-日历提醒助手.md)、[详细设计文档-日历提醒助手.md](./详细设计文档-日历提醒助手.md)  
**适用端**：Android（uni-app）  
**技术栈**：uni-app + Vue 3 + TypeScript + Vite + Pinia + Vue I18n + uni-ui  
**编写日期**：2026-08-31

## 1. 目标与边界

前端负责输入采集、端侧 OCR/解析、结果校验和编辑、系统日历写入、账号会话、局域网配对、同步队列及设置展示。电脑服务器负责账号数据、长期保存、可选的文本解析和增量同步；服务器不接收原始图片，前端不得在未确认时写入日历。

V1 仅支持 Android 8.0（API 26）及以上、单张图片、中文和英文、单/多事项、系统分享文字或图片。多人协作、公网服务、第三方云模型和原始图片云备份不在本设计范围。

## 2. 总体架构

```text
页面/组件（Vue）
        |
Pinia stores + composables（业务状态）
        |
领域服务（parse / calendar / sync / auth）
        |-----------------------------|
uni.request HttpClient             UTS 原生桥
        |                           |-- ML Kit OCR
局域网 FastAPI /api/v1              |-- CalendarContract
                                    |-- ACTION_SEND / 权限 / Keystore
```

### 2.1 运行模式

1. 默认 `parseMode=local`：图片由端侧 ML Kit OCR，文本优先使用端侧量化模型；无模型或置信度低时提示切换电脑解析。
2. 用户同意电脑解析且已配对时使用 `parseMode=server`，只上传文本、设备当前时间、语言和时区。
3. 电脑不可达、超时或服务关闭时回退端侧；同步变更进入本地 outbox，前台或后台网络恢复后重试。
4. 所有模式均返回统一 `ParseResult`，必须通过 JSON Schema、日期完整性和时间区间校验后才可进入确认页。

## 3. 工程结构

仓库采用前后端分离结构：`frontend/` 为 UniApp 前端工程，`server/` 为 Python/FastAPI 后端工程。两者独立安装依赖、启动和发布。

```text
frontend/
  package.json, vite.config.ts, pages.json, manifest.json
  src/
    pages/
      home/index.vue
      input/image.vue
      input/text.vue
      ocr/edit.vue
      parse/loading.vue
      parse/result.vue
      confirm/[requestId].vue
      calendar/success.vue
      history/index.vue
      history/detail.vue
      auth/login.vue
      auth/register.vue
      auth/verify-email.vue
      auth/reset-password.vue
      settings/index.vue
      settings/devices.vue
      settings/server.vue
    components/
      AppHeader.vue, ErrorState.vue, LoadingStage.vue
      EventCard.vue, EventEditor.vue, ConfidenceBadge.vue
      CalendarPicker.vue, ReminderPicker.vue, SourceText.vue
    stores/
      session.ts, parse.ts, draft.ts, history.ts, settings.ts, sync.ts
    services/
      http.ts, auth.ts, parse.ts, requests.ts, sync.ts, devices.ts, account.ts
      calendar.ts, health.ts
    native/
      ocr.uts, calendar.uts, share.uts, secure-storage.uts, background-sync.uts
    types/
      api.ts, domain.ts, native.ts
    utils/
      id.ts, time.ts, validation.ts, retry.ts, logger.ts, permissions.ts
    locales/zh-CN.ts, locales/en-US.ts
    App.vue, main.ts, style.css
server/
  main.py, requirements.txt, data/
```

页面层只依赖 `services`、stores 和 `native` 的 TypeScript 接口，不直接调用 Android API 或拼接 URL。前端通过 `VITE_API_BASE_URL` 访问 `server/` 提供的 FastAPI 服务。

## 4. 页面设计

### 4.1 路由和入口

| 路由 | 页面 | 入口/退出条件 | 主要状态 |
| --- | --- | --- | --- |
| `/pages/home/index` | 首页 | Tab/Home；分享回调也落到此流程 | 最近记录、服务状态 |
| `/pages/input/image` | 图片输入 | 首页“上传图片/拍摄” | 选择中、压缩中、失败 |
| `/pages/input/text` | 文字输入 | 首页“粘贴文字” | 编辑、字数超限 |
| `/pages/ocr/edit` | OCR 原文编辑 | 图片识别成功 | 原文可编辑、重识别 |
| `/pages/parse/loading` | 解析处理中 | 输入确认后 | `stage=OCR/LOCAL_PARSE/SERVER_PARSE` |
| `/pages/parse/result` | 解析结果 | 解析完成 | 单/多事件选择、低置信度警告 |
| `/pages/confirm/[requestId]` | 事件确认 | 结果页下一步 | 字段编辑、日期/日历校验 |
| `/pages/calendar/success` | 创建成功 | 日历写入完成 | 成功、部分失败、打开系统日历 |
| `/pages/history/index` | 历史/草稿 | 首页/设置 | 分页、筛选、离线标记 |
| `/pages/history/detail` | 历史详情 | 列表项点击 | 查看、编辑、重试、删除 |
| `/pages/auth/login` | 登录 | 需要账号能力时 | 登录、刷新令牌 |
| `/pages/auth/register` | 注册 | 登录页 | 邮箱、密码 |
| `/pages/auth/verify-email` | 邮箱验证 | 注册后 | 验证码倒计时 |
| `/pages/auth/reset-password` | 重置密码 | 登录页 | 验证码、新密码 |
| `/pages/settings/index` | 设置 | Tab/首页 | 语言、解析、日历、数据 |
| `/pages/settings/devices` | 设备管理 | 设置 | 配对、撤销、远程退出 |
| `/pages/settings/server` | 电脑服务 | 设置 | 地址、配对码、健康状态 |

### 4.2 首页

- 三个主入口：上传图片、粘贴文字、使用相机；入口下方显示“从其他应用分享”的最近接收状态。
- 显示端侧解析可用性、电脑服务器连接状态和待同步数量，不显示原始内容。
- 最近 5 条记录显示标题、日期、状态（已创建/草稿/失败），点击进入详情。
- 首次启动依次请求隐私说明、相机/相册权限；日历权限延迟到首次创建前申请。

### 4.3 图片输入与 OCR 编辑

1. `uni.chooseImage` 或原生相机返回 URI，校验扩展名（JPG/PNG/HEIC）和大小（<=10 MB）。
2. UTS 层通过 `ContentResolver` 读取 URI，校正 EXIF 方向，长边缩放至 2048，JPEG 质量 85，写入 `cache/{requestId}`。
3. OCR 期间显示阶段和取消按钮；成功后进入 OCR 编辑页，失败提供重选、重拍、粘贴文字三个动作。
4. OCR 编辑页保留段落和换行，文本上限 10,000 字；用户点击“解析”后删除临时图片（异常也在下次启动清理 24 小时以上目录）。

### 4.4 解析结果页

- 顶部显示解析来源（端侧/电脑）、模型版本和更新时间。
- 多事项最多展示 20 个 `EventCard`，默认全选；用户可单项勾选、编辑或移除。
- 字段置信度：`>=0.85` 正常、`0.60-0.85` 显示“需确认”、`<0.60` 标红；低置信度不阻止保存草稿，但阻止直接创建。
- `missingFields`、`warnings` 显示为可点击修复项；“无时间”明确标注“全天事件，日期待确认”。
- 操作：重试解析、编辑原文、保存草稿、确认所选事项。

### 4.5 确认页

每个事项使用 `EventEditor` 编辑：标题（必填）、日期、开始/结束时间、全天开关、地点、备注、重复、目标日历和提醒。日期选择器必须输出完整年/月/日；开始和结束时间采用设备时区展示并保留 IANA 时区。

- 识别到时间点 `T` 且无区间：默认 `start=T-1h`、`end=T+1h`、`anchorTime=T`，显示规则说明。
- 无明确时间或仅截止时间：默认 `allDay=true`，日期默认为今天但必须由用户确认；默认全天提醒 09:00。
- 时间矛盾、标题为空、日期不完整、结束不晚于开始时，禁用“创建提醒”，只保留“保存草稿”。
- 来源原文折叠展示，便于核对；文本不进入日志。

### 4.6 日历成功页

- 单项成功显示系统事件摘要和“打开系统日历”。
- 批量创建显示成功数/失败数及逐项错误；部分失败不回滚成功项，失败项可重试。
- 写入前按钮进入 `CREATING` 并禁用；根据本地映射恢复，防止重复事件。

### 4.7 历史、账号和设置

- 历史支持状态、日期筛选和分页；草稿可继续确认，失败项可重试，删除使用软删除并同步 tombstone。
- 未登录用户仅保留本机最近记录；查看服务器历史、导出、同步和设备管理时引导登录。
- 设置包括语言、默认日历、提醒策略、明确时间点规则开关、电脑解析同意、服务器地址、数据导出/删除、退出登录。
- 设备页显示设备名、最近在线、配对状态，可撤销单设备并触发令牌清理。

## 5. 状态管理与领域模型

### 5.1 页面状态机

```text
IDLE -> LOADING(stage) -> CONTENT(data)
                         -> NEEDS_CONFIRMATION(data)
                         -> ERROR(code, retryable)
```

`requestId` 是整个输入到日历写入流程的主键；进程被杀后从本地持久化恢复请求和草稿。Pinia 只保存可序列化状态，大文本和 outbox 使用 SQLite/加密文件。

### 5.2 TypeScript 核心类型

```ts
export type SourceType = 'image' | 'text' | 'share'
export type ParseMode = 'local' | 'server' | 'auto'
export type RequestStatus = 'DRAFT' | 'PARSING' | 'PARSED' | 'CREATING' | 'CREATED' | 'FAILED'

export interface ParsedEvent {
  localId: string
  title: string
  anchorTime: string | null
  startTime: string | null
  endTime: string | null
  dueTime: string | null
  allDay: boolean
  timezone: string
  location: string | null
  notes: string | null
  recurrence: string | null
  confidence: number
  missingFields: string[]
  userEdited: boolean
  reminder?: { enabled: boolean; minutesBefore: number | null }
}

export interface ParseResult {
  requestId: string
  events: ParsedEvent[]
  warnings: string[]
  modelVersion?: string
  serverTime?: string
  needsConfirmation: boolean
}

export interface ParseRequest {
  requestId: string
  text: string
  locale: string
  now: string
  timezone: string
  schemaVersion: '1.0'
  parseMode?: ParseMode
}
```

### 5.3 Store 职责

| Store | 持有状态 | 关键 action |
| --- | --- | --- |
| `session` | access/refresh token、用户、设备 | `login`、`refresh`、`logout`、`clear` |
| `parse` | 当前 request、阶段、结果、取消句柄 | `start`、`retry`、`cancel`、`selectEvent` |
| `draft` | 草稿、版本、日历映射 | `save`、`updateEvent`、`createCalendarEvents` |
| `history` | 列表、游标、筛选 | `load`、`remove`、`loadDetail` |
| `settings` | locale、解析同意、日历和提醒偏好 | `hydrate`、`update` |
| `sync` | outbox、游标、冲突、重试次数 | `enqueue`、`flush`、`pull`、`resolveConflict` |

## 6. 请求封装

### 6.1 统一响应和错误

```ts
export interface ApiEnvelope<T> { data: T; requestId?: string }
export interface ApiErrorBody { code: string; message: string; retryable: boolean; details?: unknown }
export class ApiError extends Error {
  constructor(public code: string, message: string, public retryable = false, public status?: number) {
    super(message)
  }
}
```

服务端成功返回 `{ data: ... }`；HTTP 4xx/5xx 或业务错误统一转换成 `ApiError`，页面只根据 `code` 显示本地化文案，不展示服务端堆栈。

### 6.2 HttpClient 约定

```ts
// services/http.ts
const API_PREFIX = '/api/v1'
const RETRYABLE = new Set(['SERVER_UNREACHABLE', 'MODEL_UNAVAILABLE', 'MODEL_INVALID_JSON', 'CALENDAR_WRITE_FAILED'])

export async function request<T>(
  path: string,
  options: UniApp.RequestOptions & { auth?: boolean; idempotencyKey?: string; signal?: AbortSignal } = {},
): Promise<T> {
  const session = useSessionStore()
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    'X-Client-Version': __APP_VERSION__,
    'X-Request-Id': options.idempotencyKey ?? createRequestId(),
  }
  if (options.auth !== false && session.accessToken) headers.Authorization = `Bearer ${session.accessToken}`
  if (session.deviceId) headers['X-Device-Id'] = session.deviceId

  try {
    const response = await uniRequest<T>({
      ...options, url: `${useSettingsStore().serverUrl}${API_PREFIX}${path}`,
      header: { ...headers, ...(options.header as Record<string, string> | undefined) }, timeout: 30_000,
    })
    if (response.statusCode === 401 && options.auth !== false && await session.tryRefresh()) {
      return request<T>(path, { ...options, auth: true })
    }
    if (response.statusCode < 200 || response.statusCode >= 300) throw toApiError(response)
    return (response.data as ApiEnvelope<T>).data
  } catch (error) {
    if (error instanceof ApiError) throw error
    throw new ApiError('SERVER_UNREACHABLE', '无法连接电脑服务', true)
  }
}
```

实现要求：

- 每个请求绑定 `requestId`，调用方可取消；解析超时 30 秒，普通请求 15 秒。
- 仅对 GET、`/parse`（带相同 requestId）和同步 push（带幂等键）自动重试，指数退避 1/2/4 秒，最多 2 次。
- 401 只允许单线程刷新 Token，刷新失败清空会话并跳转登录；避免无限递归。
- 服务器地址来自设置并限制为 `http(s)` 私有网段；生产联调优先局域网 HTTPS，HTTP 仅开发开关可用。
- 不在日志记录 Authorization、原文、验证码和图片 URI；性能日志只记录耗时、状态码和错误码。

### 6.3 Service API

```ts
// services/parse.ts
export const parseText = (body: ParseRequest, signal?: AbortSignal) =>
  request<ParseResult>('/parse', { method: 'POST', data: body, signal, idempotencyKey: body.requestId })

// services/auth.ts
export const register = (data: { email: string; password: string }) => request('/auth/register', { method: 'POST', data, auth: false })
export const verifyEmail = (data: { email: string; code: string }) => request('/auth/verify-email', { method: 'POST', data, auth: false })
export const login = (data: { email: string; password: string }) => request('/auth/login', { method: 'POST', data, auth: false })
export const refresh = (refreshToken: string) => request('/auth/refresh', { method: 'POST', data: { refreshToken }, auth: false })
export const logout = () => request('/auth/logout', { method: 'POST' })
```

### 6.4 API 契约

接口前缀为 `/api/v1`，请求/响应 UTF-8 JSON，时间使用带偏移的 ISO 8601。下表是前端必须实现的 V1 服务；标记“规划”的接口需以后端完成后启用，前端应显示能力开关而不是直接报错。

| 方法 | 路径 | 前端用途 | 当前联调 |
| --- | --- | --- | --- |
| POST | `/auth/register` | 注册并发送验证码 | 已有（当前不要求验证） |
| POST | `/auth/verify-email` | 验证邮箱 | 规划 |
| POST | `/auth/login` | 登录 | 已有 |
| POST | `/auth/refresh` | 刷新令牌 | 规划（当前 token 30 天） |
| POST | `/auth/logout` | 撤销会话 | 已有 |
| POST | `/auth/reset-password` | 重置密码 | 规划 |
| POST | `/devices/pair/start` | 获取二维码/配对码 | 规划 |
| POST | `/devices/pair/complete` | 完成配对 | 规划 |
| GET | `/devices` | 设备列表 | 规划 |
| DELETE | `/devices/{id}` | 撤销设备 | 规划 |
| POST | `/parse` | 电脑文本解析 | 已有（规则回退） |
| GET | `/requests/{id}` | 恢复解析请求 | 已有 |
| POST | `/sync/pull` | 增量拉取 | 规划 |
| POST | `/sync/push` | 上传离线变更 | 规划 |
| GET | `/export?format=json|csv` | 导出数据 | 规划 |
| POST | `/backup` | 创建加密备份 | 规划 |
| DELETE | `/account` | 注销并删除数据 | 规划 |
| GET | `/health` | 服务、数据库、模型状态 | 已有 |

#### `/parse` 请求和响应

```json
{
  "requestId": "req_01H...",
  "text": "9月1日下午2点提交报销单",
  "locale": "zh-CN",
  "now": "2026-08-31T10:00:00+08:00",
  "timezone": "Asia/Shanghai",
  "schemaVersion": "1.0",
  "parseMode": "server"
}
```

响应为 `{ data: { requestId, events, warnings, modelVersion, serverTime, needsConfirmation } }`。相同用户和 `requestId` 必须幂等返回相同结果。

#### 同步请求

`sync/pull`：`{ cursor, limit }`，返回 `changes[]`、`nextCursor`。  
`sync/push`：`{ changes: [{ entityType, entityId, operation, baseVersion, payload, clientUpdatedAt }] }`，返回 `accepted[]`、`conflicts[]`、`nextCursor`。

#### 当前服务端联调适配

仓库中的 `server/main.py` 是可运行的规则解析回退服务，响应错误仍可能是 FastAPI 默认的 `{ "detail": "..." }`，且 `/parse` 当前不识别 `schemaVersion/parseMode`。`http.ts` 需要兼容两种错误形态；`services/parse.ts` 在开发环境可省略后端尚未支持的可选字段。收到旧响应后由 `normalizeParseResult()` 补齐 `localId`、`userEdited=false`、`reminder`，并将缺省 `sourceType` 设为 `text`。后端升级到 V1 契约后切换 `VITE_API_CONTRACT_VERSION`，不改变页面和 Store 接口。

## 7. 原生 Android 能力

所有原生能力定义 Promise 风格接口，页面不感知实现差异；非 Android 平台提供拒绝或空实现，便于 H5 预览。

```ts
export interface OcrPlugin {
  recognize(uri: string, options: { locale: string }): Promise<{ text: string; blocks: OcrBlock[] }>
}
export interface CalendarPlugin {
  requestPermission(): Promise<boolean>
  listCalendars(): Promise<CalendarInfo[]>
  createEvent(input: CalendarEventInput): Promise<{ eventId: string }>
  updateEvent(eventId: string, input: CalendarEventInput): Promise<void>
  openEvent(eventId: string): Promise<void>
}
export interface SharePlugin { consumeInitialShare(): Promise<SharePayload | null>; onNewShare(cb: (p: SharePayload) => void): () => void }
```

- **OCR**：ML Kit 中文/Latin；使用 `ContentResolver` 读取 `content://` URI，不假设真实文件路径。
- **Calendar**：申请 `READ_CALENDAR`/`WRITE_CALENDAR`；查询 `Calendars`，写入 `Events` 和 `Reminders`。全天事件按设备支持情况降级并提示。
- **系统分享**：Android `ACTION_SEND` 接收 `text/plain`、`image/*` 和文件 URI，只处理首个附件，统一转为 `ParseCoordinator`。
- **安全存储**：Token、deviceId、服务器地址使用 Android Keystore 加密；普通偏好可使用 `uni.storage`。
- **后台同步**：使用 WorkManager/UTS 插件，网络可用时按 1、2、4、8、16 分钟退避，最多 6 次；系统限制时以前台打开应用同步为准。
- **权限跳转**：拒绝后调用系统设置页；页面展示用途和重新授权按钮。

## 8. 解析编排与校验

`ParseCoordinator` 统一处理文字、图片和分享输入：

1. 生成 `requestId`，保存原文和来源到本地请求表。
2. 图片输入先 OCR；文本做空白、日期分隔符和语言标准化。
3. 规则解析器提取绝对时间，再按设置选择端侧或电脑模型；电脑调用失败回退端侧。
4. 使用 JSON Schema 校验：标题 1-200 字符、置信度 0-1、IANA 时区、ISO 时间、结束晚于开始、最多 20 项。
5. 补齐 `missingFields` 和 `warnings`，保存 `PARSED` 草稿；任何校验失败都不能创建日历。
6. 用户在确认页修改后标记 `userEdited=true`，重新执行领域校验；通过后调用 `CalendarRepository`。

日期规则：缺少年/月/日必须要求用户选择；仅日期或仅截止时间默认全天；明确时间点 T 且无区间使用 T-1h/T+1h；已有区间保持原值；跨午夜允许结束日期为次日。

## 9. 日历写入、幂等和恢复

1. 创建按钮点击即进入 `CREATING`，计算 `hash(userId, deviceId, requestId, localId)` 作为幂等键。
2. 查询本地 `calendar_mappings`；已有有效 `systemEventId` 时只执行更新，不重复创建。
3. 首次写入前申请权限并让用户选择目标日历，保存 `calendarId`；日历失效时要求重新选择。
4. 成功保存 `calendarId/systemEventId/writeTime`；失败保留草稿和错误码。
5. 批量写入逐项执行，部分失败不回滚；成功页提供逐项重试。
6. 应用崩溃恢复时先查映射再重试；系统日历中事件被用户删除时标记 `ORPHANED`，不自动重建。

提醒映射：具体时间事件默认 `Reminders.minutes=0`；全天事件默认当天 09:00（厂商不支持时使用系统默认）；关闭提醒时不写入 `Reminders`。

## 10. 本地存储与同步

### 10.1 存储分层

| 数据 | 存储 | 生命周期 |
| --- | --- | --- |
| Token、设备密钥 | Keystore 加密 | 直到退出/撤销 |
| 语言、偏好、最近服务器 | `uni.storage` | 用户清除前 |
| 请求、草稿、事件、映射、outbox | SQLite 原生插件 | 用户删除或注销 |
| OCR 临时图片 | `cache/{requestId}` | OCR 后立即删，启动清理 >24h |

### 10.2 outbox

每条记录包含 `id、entityType、entityId、operation、payload、baseVersion、retryCount、nextRetryAt、lastError`。上传成功删除；冲突进入 `CONFLICT`，保留服务端和本地副本供历史详情页合并。删除使用 tombstone，避免离线设备复活旧数据。

## 11. 异常、可用性和可访问性

统一错误码：`AUTH_INVALID`、`EMAIL_NOT_VERIFIED`、`SERVER_UNREACHABLE`、`MODEL_UNAVAILABLE`、`MODEL_INVALID_JSON`、`DATE_NOT_CONFIRMED`、`CALENDAR_PERMISSION_DENIED`、`CALENDAR_WRITE_FAILED`、`SYNC_CONFLICT`、`STORAGE_FULL`、`IMAGE_TOO_LARGE`、`OCR_EMPTY`、`IMAGE_DECODE_FAILED`。

页面必须提供重试、编辑原文、保存草稿或手动创建路径；网络和电脑服务不可用不阻塞端侧流程。所有关键控件支持 TalkBack contentDescription、焦点顺序、动态字体和深色模式；字号放大时不重叠。加载页可取消且取消后保留草稿。

## 12. 安全与隐私实现

- 默认端侧处理；原图不上传，服务端仅接收文本和结构化字段。
- API 携带 Bearer Token、`X-Device-Id`、时间戳和 nonce；Token 过期自动刷新，退出登录清理本地令牌。
- 服务器地址限制私有网段，首次配对使用 5 分钟一次性码/二维码；用户可随时撤销设备。
- 不记录密码、验证码、Token、完整原文和图片路径；导出/注销需二次验证。
- 解析模式、历史保存、电脑服务同意均可在设置中撤销；撤销后清理相应本地队列。

## 13. 测试与验收

- **单元**：时间算法、日期完整性、JSON Schema、置信度、幂等 hash、请求重试和 Token 刷新并发。
- **组件/页面**：表单校验、低置信度提示、多事项勾选、确认页禁用条件、无权限恢复、深色模式和动态字号。
- **集成**：当前 `server/main.py` 的 `/health`、注册、登录、解析、请求查询、登出；规划接口用 Mock Service Worker/本地 fixture 占位。
- **真机**：Android 8-15、AOSP/Google/厂商日历、相机/相册权限、HEIC、系统分享、断网、进程重启、WorkManager、TalkBack。
- **验收指标**：OCR 普通图片 <=3 秒，端侧解析 <=5 秒；重复创建率 <1%；明确时间区间和无时间全天规则与 PRD 一致。

## 14. 构建、配置与发布

Vite 环境变量：`VITE_API_BASE_URL`、`VITE_API_TIMEOUT`、`VITE_ENABLE_SERVER_PARSE`、`VITE_SCHEMA_VERSION`。Android `minSdk=26`；发布包不内置超过设备基线的模型，模型通过可校验的离线资源包安装并显示磁盘占用。

发布物：Android APK、原生插件版本清单、服务器地址/配对说明、隐私说明、OpenAPI 文档、数据库迁移和备份脚本、测试报告。生产构建关闭调试日志和 HTTP 明文开关。

## 15. 待补充与需确认事项

以下决定会影响页面和接口，需产品/后端确认后冻结：

1. 邮箱验证服务：当前服务端注册接口直接返回 `verificationRequired=false`，是否在 V1 强制真实邮件验证码？邮件 SMTP、验证码模板和发送频率由谁提供？
2. `/devices/pair/start` 与 `/pair/complete` 的二维码字段、设备命名规则和配对失败处理。
3. 局域网 HTTPS 证书分发/信任方案；是否允许开发环境 HTTP，正式环境是否要求用户导入自签名证书？
4. 端侧 Qwen 模型的最终量化格式、下载/升级/回滚、最低 RAM 和磁盘空间检测。
5. Android 厂商对全天 09:00 提醒和 `Reminders` 的兼容策略，是否允许改用系统默认提醒并如何向用户告知。
6. 历史分页大小、保存期限、批量删除上限、导出文件加密密码和备份恢复确认流程。
7. 同步冲突页面的合并粒度（字段级或整条覆盖）、最后修改时间来源及设备时钟漂移容忍度。
8. 分享扩展在应用未启动、锁屏和多附件场景的产品行为；V1 是否始终只取首个附件。
9. 是否需要应用内提醒/通知作为系统日历不可用时的兜底；若需要，需新增 `POST_NOTIFICATIONS` 权限和通知渠道设计。
10. 匿名性能指标是否允许上传到电脑服务器之外的收集端，以及用户关闭诊断数据后的行为。

## 16. 与需求追踪

| 需求 | 前端实现 |
| --- | --- |
| 图片/文字/分享三入口 | 首页、图片输入、文字输入、SharePlugin |
| OCR 可编辑 | ML Kit 原生桥 + OCR 编辑页 |
| 明确时间前后 1 小时 | `ParseCoordinator` 时间规则 + 确认页说明 |
| 无时间默认全天 | 全天开关、日期确认、09:00 提醒映射 |
| 用户确认后写入 | 确认页校验和 `CREATING` 状态机 |
| 电脑可选、端侧回退 | parseMode、健康检查和 outbox |
| 登录、跨设备同步 | session、devices、sync stores 与接口 |
| 隐私、可恢复和幂等 | Keystore、临时文件清理、calendar_mappings |
