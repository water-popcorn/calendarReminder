# 日历提醒助手详细设计文档

文档版本：V1.0  
依据文档：产品需求文档-日历提醒助手.md、概要设计文档-日历提醒助手.md  
适用范围：uni-app Android V1（MVP）与电脑局域网服务器 V1  
编写日期：2026-08-29

## 1. 设计范围

本文件细化手机端、电脑服务器、账号、同步、模型解析、系统日历写入、备份、安全和测试。V1 不实现多人协作、公网访问、第三方云模型和原始图片云备份。

## 2. 工程结构

### 2.1 uni-app 工程

~~~text
uniapp/
  pages/, components/, layouts/
  stores/, composables/, locales/
  services/api.ts, services/parse.ts, services/sync.ts
  native/ocr.uts, native/calendar.uts, native/share.uts
  utils/time.ts, utils/validation.ts, utils/crypto.ts
  uni_modules/, manifest.json, pages.json
~~~

### 2.2 电脑服务器工程

~~~text
server/app/
  main.py, config.py
  api/auth.py, devices.py, parse.py, sync.py, export.py, health.py
  core/auth_service.py, pairing_service.py, parse_service.py
  core/sync_service.py, backup_service.py, audit_service.py
  db/connection.py, models.py, migrations/
  model/prompt_builder.py, json_validator.py, runtime.py
server/data/calendar.db, backups/, logs/
server/scripts/start.ps1, stop.ps1, status.ps1, backup.ps1
~~~

## 3. uni-app 客户端详细设计

### 3.1 模块职责

| 模块 | 职责 |
| --- | --- |
| pages/components | 首页、输入、OCR 编辑、解析结果、确认、历史、设置、登录 |
| input | 相册、相机、剪贴板和 Android Sharesheet |
| native/ocr | 通过 UTS/uni 原生插件调用 ML Kit |
| parser | 端侧 Qwen 原生插件、提示词、JSON 校验 |
| native/calendar | 通过 UTS/uni 原生插件调用 CalendarContract |
| sync | 局域网请求、配对、登录会话和离线队列 |
| storage | uni.storage、SQLite 原生插件和加密文件 |
| common | 多语言、时间、权限、错误码和日志 |

推荐技术栈：uni-app（Vue 3 + TypeScript）、Vite、Pinia、Vue I18n、uni-ui；原生能力通过 UTS/uni 原生插件封装，服务器使用 Python 3.11 + FastAPI。

### 3.2 页面和状态

路由：home、input/image、input/text、ocr/edit、parse/loading、parse/result、confirm/{requestId}、calendar/success、history、auth/login、auth/register、settings、settings/devices。

页面状态统一为 Idle、Loading(stage)、Content(data)、NeedsConfirmation(data)、Error(code, retryable)。页面切换和进程重启后通过 Pinia 持久化与本地 SQLite 恢复 requestId、草稿和队列。

### 3.3 图片和分享

- 通过 UTS/Android 原生扩展接收 ACTION_SEND 的 text/plain、image/* 和 content URI；V1 只处理首个附件。
- 读取 URI 使用 ContentResolver，不依赖真实文件路径。
- EXIF 方向校正，长边超过 2048 像素时缩放，JPEG 质量 85。
- 临时目录为 cache/requestId，OCR 完成后删除；启动时清理超过 24 小时的目录。
- 图片超过 10 MB、无法解码或 OCR 为空分别返回 IMAGE_TOO_LARGE、IMAGE_DECODE_FAILED、OCR_EMPTY。
- 分享内容与首页输入共用同一 ParseCoordinator。

### 3.4 本地解析流程

1. 清理空白、规范日期分隔符并识别语言。
2. 优先端侧模型；用户选择电脑解析或端侧置信度低时调用服务器。
3. 规则解析器先识别绝对时间，再由模型处理相对时间、标题、地点、备注和多事项拆分。
4. 使用固定 JSON Schema 校验，错误时最多重试一次。
5. 日期不完整或时间矛盾时仅保存草稿，不允许写入日历。
6. 解析结果进入确认页，用户确认后才调用 CalendarRepository。

## 4. 时间与事项规则

### 4.1 领域对象

ParseRequest：requestId、sourceType、sourceText、locale、now、timezone、parseMode。  
ParsedEvent：localId、title、anchorTime、startTime、endTime、dueTime、allDay、timezone、location、notes、recurrence、confidence、missingFields、userEdited。

### 4.2 时间算法

1. 缺少年、月或日时写入 missingFields，必须由用户在确认页补齐。
2. 仅有日期时设置 allDay=true，事件日期为用户确认的完整年月日。
3. 有明确时间点 T 且没有区间时，startTime=T-1 小时、endTime=T+1 小时、anchorTime=T。
4. 已有明确开始和结束时保留原区间，不套用前后 1 小时规则。
5. 只有截止时间时保存 dueTime，默认按全天事件处理。
6. “上午/下午”等模糊时段生成候选值并加入 warnings，仍需用户确认。
7. 结束时间必须晚于开始时间；跨午夜事件允许结束日期为次日。

### 4.3 多事项

模型最多返回 20 个 events。每项使用稳定 localId。批量创建逐项执行，部分失败不回滚成功项，并显示明细。

## 5. 模型设计

### 5.1 模型选择

- 手机端：Qwen2.5-1.5B-Instruct 量化版本，目标支持内存 4 GB 以上设备。
- 电脑端：同系列更大或量化模型，由电脑 CPU/GPU 和内存测试决定。
- OCR：ML Kit 中文和 Latin 识别。

### 5.2 提示词

~~~text
你是日历事项抽取器，只输出合法 JSON。
当前时间：{now}；时区：{timezone}；语言：{locale}。
不得臆造缺失的年、月、日；无时间返回 allDay=true。
明确时间点 T 且无区间时返回 startTime=T-1h、endTime=T+1h、anchorTime=T。
~~~

用户原文作为独立数据段传入，限制最大 token。

### 5.3 JSON 校验

根对象必须有 events 和 warnings。title 长度 1 到 200，confidence 为 0 到 1，时间为 ISO 8601，timezone 为 IANA 时区。allDay=true 时使用日期边界。校验失败返回 MODEL_INVALID_JSON。

置信度大于等于 0.85 正常展示，0.60 至 0.85 显示需确认，小于 0.60 标红但允许保存草稿。

## 6. 系统日历写入

### 6.1 边界和权限

- 仅 uni-app Android 原生日历插件调用 CalendarContract，电脑不直接写手机日历。
- 首次写入申请 READ_CALENDAR 和 WRITE_CALENDAR。
- 查询 Calendars 表展示可编辑日历，保存 calendarId。

### 6.2 事件字段映射

TITLE 对应标题，DESCRIPTION 对应备注和必要的原文摘要，EVENT_LOCATION 对应地点，DTSTART/DTEND 对应开始和结束时间，ALL_DAY 对应全天标记，EVENT_TIMEZONE 对应 IANA 时区，HAS_ALARM 对应提醒开关。

具体事件默认 Reminders.minutes=0，即事件开始时提醒。全天事件默认当天 09:00；若厂商日历不支持全天具体时刻，降级为系统默认提醒并在确认页提示。用户关闭提醒时不写 Reminders。

### 6.3 幂等和更新

- 点击创建后立即将状态改为 CREATING 并禁用按钮。
- 幂等键为 hash(userId, deviceId, requestId, localEventId)。
- 成功后保存 calendarId、systemEventId 和写入时间，状态改为 CREATED。
- 崩溃恢复时先查映射再决定重试，避免重复创建。
- 系统事件被用户删除时标记 ORPHANED，不自动重建。

## 7. 电脑服务器设计

### 7.1 服务模块

API 网关负责鉴权、参数校验、限流和统一错误。认证服务负责邮箱验证码、Argon2id 密码哈希、登录、刷新令牌和注销。设备服务负责配对和撤销。解析服务负责模型运行和 JSON 校验。同步服务负责游标、版本和冲突。数据服务负责 SQLite、导出、备份和恢复。运维服务提供健康检查、日志和磁盘告警。

### 7.2 进程和运行

服务器默认绑定局域网 IP，不开放公网；Windows 使用计划任务开机启动。启动顺序：读取配置、执行迁移、检查磁盘、加载模型、启动 API。电脑不可达时手机端侧解析可用，同步写入本地 outbox。

### 7.3 后端语言与框架定案

后端统一采用 Python 3.11。Web 层使用 FastAPI + Uvicorn，数据校验使用 Pydantic，数据库访问使用 SQLAlchemy 2.0（或 SQLModel），迁移使用 Alembic，密码哈希使用 argon2-cffi，测试使用 pytest。选择 Python 的主要原因是本地 Qwen/llama.cpp 推理、文本处理和日期解析生态成熟，便于在个人电脑上快速部署和调试；V1 的局域网并发量较低，不需要 Java 的高并发优势。后续若演进为公网高并发服务，可保持 API 契约不变再拆分 Java 服务。

服务器采用异步 API + 同步模型工作线程：FastAPI 处理网络请求，模型推理放入独立线程或进程，避免阻塞事件循环；SQLite 写入通过单独事务会话完成。

## 8. 账号和设备配对

- 注册接口接收邮箱和密码并发送验证码；验证码 10 分钟有效，错误 5 次锁定 15 分钟。
- 密码最少 10 位，使用 Argon2id 保存；登录失败和验证码接口限流。
- Access Token 15 分钟，Refresh Token 最长 30 天且可撤销。
- 配对码 5 分钟有效，只显示一次；二维码只包含服务器地址、pairingId 和短期 code。
- 手机保存 serverUrl、deviceId 和加密 deviceToken；服务器只保存令牌哈希。
- 请求携带 Authorization、X-Device-Id、X-Timestamp、X-Nonce；时间偏差超过 5 分钟或 nonce 重复则拒绝。

## 9. 数据库详细设计

### 9.1 users

id、email（唯一不区分大小写）、password_hash、email_verified、locale、created_at、updated_at、deleted_at。

### 9.2 devices

id、user_id、name、platform、pairing_state、token_hash、last_seen_at、created_at、revoked_at。

### 9.3 requests

id、user_id、request_id、source_type、source_text、language、parse_mode、status、model_version、created_at、updated_at、deleted_at。

### 9.4 events

id、user_id、request_id、title、anchor_time、start_time、end_time、due_time、all_day、timezone、location、notes、recurrence、confidence、missing_fields_json、user_edited、version、created_at、updated_at、deleted_at。

### 9.5 calendar_mappings

id、event_id、device_id、calendar_id、system_event_id、write_status、last_written_at。

### 9.6 sync_changes

id、user_id、entity_type、entity_id、operation、version、updated_at、device_id；对删除记录保留 tombstone 30 天。

索引：users.email 唯一索引；events(user_id, updated_at)；events(user_id, request_id)；sync_changes(user_id, id)。

## 10. API 详细设计

接口前缀 /api/v1，使用 UTF-8 JSON 和 ISO 8601 时间。成功响应包含 data、requestId；错误响应包含 code、message、retryable。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /auth/register | 邮箱注册并发送验证码 |
| POST | /auth/verify-email | 验证邮箱 |
| POST | /auth/login | 邮箱密码登录 |
| POST | /auth/refresh | 刷新令牌 |
| POST | /auth/logout | 撤销会话 |
| POST | /auth/reset-password | 重置密码 |
| POST | /devices/pair/start | 创建配对码 |
| POST | /devices/pair/complete | 完成配对 |
| GET | /devices | 设备列表 |
| DELETE | /devices/{id} | 撤销设备 |
| POST | /parse | 电脑模型解析文本 |
| GET | /requests/{id} | 查询解析结果 |
| POST | /sync/pull | 增量拉取 |
| POST | /sync/push | 上传本地变更 |
| GET | /export | 导出数据 |
| POST | /backup | 创建加密备份 |
| DELETE | /account | 注销并删除账号 |
| GET | /health | 健康检查 |

### 10.1 /parse

请求字段：requestId、text、locale、now、timezone、schemaVersion。文本最大 10,000 字符，超时 30 秒。相同 userId 和 requestId 返回同一结果。

响应字段：requestId、events、warnings、modelVersion、serverTime、needsConfirmation。

### 10.2 /sync/push

每条变更包含 entityType、entityId、operation、baseVersion、payload。服务器当前版本等于 baseVersion 才接受，否则返回冲突双方摘要。

## 11. 同步算法

1. 登录并配对后获取 deviceId 和令牌。
2. 客户端提交游标，服务器返回游标之后的变更。
3. 客户端按顺序上传 outbox；服务端进行版本校验。
4. 接受的变更生成新版本和 sync_change。
5. 冲突保留服务器版本和客户端副本，用户选择后再次提交。
6. uni-app 前台生命周期配合原生后台任务插件在网络可用时重试；退避 1、2、4、8、16 分钟，最多 6 次。
7. 删除使用 tombstone，防止离线设备复活旧数据。

## 12. 备份、恢复和长期保存

- 数据库、配置和令牌备份为加密压缩包，AES-256-GCM 加密。
- 默认每日备份到用户指定目录，至少保留最近 7 个版本；备份失败写入日志并提示磁盘空间。
- 恢复前校验文件完整性、版本和用户确认；恢复后执行迁移和健康检查。
- 导出提供 JSON 和 CSV；原始图片不在服务器数据中。
- 注销要求再次验证密码或验证码，先软删除再后台物理清理。

## 13. 错误码

| 错误码 | 含义 | 重试 |
| --- | --- | --- |
| AUTH_INVALID | 邮箱或密码错误 | 否 |
| EMAIL_NOT_VERIFIED | 邮箱未验证 | 否 |
| VERIFY_CODE_EXPIRED | 验证码过期 | 否 |
| DEVICE_NOT_PAIRED | 设备未配对 | 否 |
| SERVER_UNREACHABLE | 电脑不可达 | 是 |
| MODEL_UNAVAILABLE | 模型不可用 | 是 |
| MODEL_INVALID_JSON | 模型 JSON 非法 | 是 |
| DATE_NOT_CONFIRMED | 日期未确认 | 否 |
| CALENDAR_PERMISSION_DENIED | 日历权限拒绝 | 否 |
| CALENDAR_WRITE_FAILED | 日历写入失败 | 是 |
| SYNC_CONFLICT | 同步冲突 | 否 |
| STORAGE_FULL | 存储空间不足 | 否 |

## 14. 安全与日志

- 局域网 API 仅绑定私有网卡，可配置允许网段和一键停止。
- 所有 SQL 使用参数化查询，并按 user_id 做对象级权限校验。
- 日志禁止记录密码、令牌、验证码和完整原文；审计日志只记录操作类型、用户 ID、设备 ID、时间和结果。
- 账号导出、注销和删除需要二次验证。
- 日志分错误、审计、性能三类，默认仅保存在电脑。

## 15. 测试设计

### 15.1 单元测试

覆盖时间解析、T-1h/T+1h、全天事件、日期完整性、JSON Schema、置信度、幂等键、加密存储。

### 15.2 集成测试

覆盖注册验证、登录重置、设备配对、端侧/服务器解析切换、同步冲突、断网重试、备份恢复和账号注销。

### 15.3 真机兼容性

覆盖 Android 8.0 以上、AOSP/厂商/Google 日历、相机相册权限、系统分享、深色模式、TalkBack 和不同语言。

### 15.4 安全测试

覆盖过期和撤销令牌、越权访问、重放 nonce、暴力登录、备份泄露、日志泄露和临时图片清理。

## 16. 配置和发布

服务器配置：SERVER_HOST、SERVER_PORT（默认 21512）、DATABASE_PATH、MODEL_PATH、MODEL_BACKEND、ACCESS_TOKEN_MINUTES、REFRESH_TOKEN_DAYS、PAIRING_CODE_MINUTES、BACKUP_DIR、LOG_LEVEL。

Windows 提供 start、stop、status、backup 脚本；Android minSdk=26，默认语言 zh-CN，服务器地址和令牌存入加密存储。

发布交付：Android APK、服务器可执行包、模型安装说明、数据库迁移和备份脚本、OpenAPI 文档、隐私说明、测试报告。

## 17. 实施顺序

1. uni-app 工程、服务器骨架、健康检查和局域网连接。
2. 图片/文字/系统分享输入、OCR 和端侧解析。
3. 结果确认、提醒编辑和系统日历写入。
4. 邮箱账号、设备配对、SQLite 和本地加密。
5. 增量同步、冲突处理、备份恢复和注销。
6. 电脑 Qwen 服务、端侧/服务器切换和中英文资源。
7. 真机、性能、安全和模型准确率测试。
