# 日历提醒助手产品需求文档（PRD）

**文档版本**：V1.1（需求确认稿）  
**编写日期**：2026-08-28  
**产品形态**：Android 手机应用  
**目标用户**：需要快速把图片、聊天消息或复制文本转换为日历提醒的个人用户

## 1. 产品概述

### 1.1 产品目标

用户将包含待办事项的图片上传到应用，或从其他应用复制文字后粘贴，应用自动识别其中“要做什么、何时做、在哪里做、相关备注”等信息，并生成可确认的日历提醒事件：

- 文本中包含明确时间：以该时间点为中心生成前后各 1 小时的日历事件，并建议创建。
- 文本没有时间：默认按用户选择的日期创建全天日历事件，也允许用户改为具体时间。
- 信息不完整或识别置信度低：展示原文和解析结果，要求用户确认或修改，禁止静默写入日历。

### 1.2 核心价值

把“看到信息”到“形成可靠提醒”缩短为几步操作，降低手动复制、切换日历、填写事件的成本，并尽量在设备端处理内容以保护隐私。

### 1.3 非目标（V1 不包含）

- 不替代完整的日历、任务管理或项目协作软件。
- 不自动发送短信、邮件或社交平台消息。
- 不在用户未确认时修改或删除已有日历事件。
- 不承诺识别所有手写体、复杂表格或低清晰度图片。
- 不提供多人共同编辑同一事项的协作能力（但支持个人账号跨设备同步）。

## 2. 术语和对象

- **输入内容**：上传的图片或粘贴的纯文本。
- **原文**：OCR 得到的文字或用户粘贴的文字。
- **事项（Task）**：用户需要完成的动作，例如“提交报销单”。
- **时间信息**：开始时间、结束时间、截止时间、重复规则及其时区。
- **提醒事件（Calendar Event）**：写入 Android 系统日历的事件记录。
- **解析结果**：模型从原文提取出的结构化字段及置信度。

## 3. 用户故事

1. 作为用户，我希望从聊天截图生成提醒，不必手动输入事项和时间。
2. 作为用户，我希望复制一段通知文字后直接粘贴解析。
3. 作为用户，当文本只有“尽快处理”而没有时间时，我希望系统按选定日期生成全天日历事件，也可以手动改成具体时间。
4. 作为用户，我希望在写入日历前看到并修改标题、时间、地点和备注。
5. 作为用户，我希望明确知道哪些数据会发送到电脑服务器，并可关闭服务器处理、改用端侧解析。
6. 作为用户，我希望在手机、平板等设备登录同一账号后看到自己的解析记录和提醒草稿。
7. 作为用户，我希望从微信、浏览器等应用通过系统分享面板直接发送图片或文字到本应用。

## 4. 功能范围

### 4.1 首页与输入

- 提供三个主入口：**上传图片**、**粘贴文字**、**从其他应用分享**。
- 图片来源：相册、相机（需用户授权）。
- 支持 JPG、PNG、HEIC；单张图片，建议不超过 10 MB。
- 文本输入支持粘贴、编辑和清空；建议上限 10,000 字符。
- 支持 Android Sharesheet 接收 `text/plain`、图片和文件 URI；分享内容进入与首页相同的解析流程。
- 首次启动可选择中文或 English，设置中可随时切换；日期、时间、权限说明、错误提示和模型提示词随语言切换。
- 首次使用时说明日历权限、相机/相册权限和数据处理方式。

### 4.2 图片识别（OCR）

- 对图片进行方向校正、裁剪和压缩后识别中英文文本；根据当前界面语言或自动检测选择识别脚本。
- 展示 OCR 原文，允许用户直接修改识别错误。
- 低清晰度、无文字或识别失败时给出可操作提示（重拍、重新选择、改用粘贴文字）。
- OCR 结果需保留段落顺序，尽可能保留日期、时间、标点和换行。

### 4.3 事项与时间解析

从原文提取以下字段，并返回字段级置信度：

| 字段 | 说明 | 示例 |
| --- | --- | --- |
| title | 必填，动作或事件名称 | 提交季度报销单 |
| startTime | 开始时间，可空 | 2026-09-01 09:00 |
| endTime | 结束时间，可空 | 2026-09-01 10:00 |
| dueTime | 截止时间，可空 | 2026-09-01 18:00 |
| anchorTime | 明确识别到的时间点 T，可空 | 2026-09-01 14:00 |
| allDay | 是否全天事件 | false |
| location | 地点，可空 | A 栋 302 会议室 |
| notes | 原文中有用的补充信息 | 携带身份证和合同 |
| recurrence | 重复规则，可空 | 每周一 |
| timezone | 时区，默认设备时区 | Asia/Shanghai |

解析规则：

- 支持绝对时间（“9 月 1 日 14:00”）和相对时间（“明天下午三点”）。
- 相对时间以设备当前时间和时区为基准，并在确认页显示换算后的绝对时间。
- 仅有日期时默认全天事件；仅有“上午/下午”等模糊时段时生成建议时间并标注“需确认”。
- 出现多个事项时，拆分为多个候选事件，用户可逐条选择创建。
- 无明确时间时不得擅自推断，默认进入“全天事件”流程；用户可主动切换为具体开始/结束时间。
- 明确时间点规则：若识别到时间点 T，则默认事件开始时间为 T-1 小时、结束时间为 T+1 小时，`anchorTime` 保存原始时间点。提醒使用手机系统日历的原生提醒能力，默认在事件开始时提醒（即 T-1 小时）；用户可在确认页调整提醒，不额外创建应用自有通知。
- 日期完整性规则：创建事件必须具备用户确认的年、月、日。模型可以根据上下文提出年份建议，但不得静默补全；缺少任一年/月/日时，确认页必须要求用户补充后才能写入日历。
- 无法确定标题时，使用原文首句作为候选标题并标注低置信度。

### 4.4 确认与编辑

- 确认页展示：标题、日期、时间（含前后 1 小时区间）、是否全天、地点、备注、重复规则、目标日历和提醒方式。
- 所有字段可编辑；高风险字段（时间、目标日历）需明显展示。
- 显示“来源原文”折叠区，方便核对。
- 提供操作：**创建提醒**、**保存为草稿**、**返回修改**、**取消**。

### 4.5 无时间事项补全

- 若缺少时间，日期默认为今天，事件类型默认为全天；用户可修改日期或切换为具体开始/结束时间。
- 全天事件支持手机系统日历提醒（默认当天 09:00，可选不提醒或提前 1 天）；具体时间事件支持准时、提前 5/10/15/30 分钟、1 小时、1 天。最终提醒行为以设备自带日历对 Calendar Provider 的支持为准。
- 用户完成选择后回到统一确认页；确认页必须明确显示“全天”。

### 4.6 日历写入

- 通过 Android Calendar Provider 写入手机系统自带日历（如 AOSP Calendar、厂商日历或 Google Calendar 提供的系统日历账户），首次写入前请求日历读写权限。
- 允许用户选择设备上的目标日历，记住上次选择。
- 创建成功后展示事件摘要和“打开系统日历”按钮。
- 重复点击创建不得产生重复事件；使用本地请求 ID 做幂等控制。
- 日历不可用、权限拒绝或写入失败时，保留草稿并提供重试及复制到剪贴板选项。

### 4.7 账号、同步、历史与设置

- 首次登录使用邮箱注册账号并设置密码，并完成邮箱验证码验证；后续使用邮箱和密码登录。V1 提供找回/重置密码、退出登录和账号注销，不要求手机号或第三方 OAuth。
- 未登录用户可试用端侧解析；保存电脑服务器上的草稿、历史或跨设备同步前提示登录。
- 最近解析记录：时间、标题、状态（已创建/草稿/失败），登录后同步到电脑服务器；电脑不可连接时写入本地队列，恢复连接后自动同步。
- 同步范围：用户创建的草稿、解析结果、偏好设置和本应用生成的事件映射；只同步文字和结构化字段，不上传或长期保存原始图片。
- 数据部署：开发和试用阶段由用户自己的电脑运行后端、数据库和解析服务，手机通过同一局域网访问电脑的受控 IP 和端口；默认不开放公网访问，不要求域名。
- 数据保存：账号、结构化提醒、草稿和历史记录默认长期保存在用户电脑上，直至用户主动删除或注销账号；系统需提供单条删除、批量清除、导出、数据库备份和账号注销后的数据删除流程。
- 电脑服务需支持开机启动、局域网设备配对、服务状态查看和安全停止；更换电脑时可通过加密备份迁移账号与文字数据。
- 密码不得明文保存，服务端使用 Argon2id 或 bcrypt 等强哈希并配合登录限流、验证码和会话令牌轮换。
- 冲突策略：以最后修改时间为主，保留冲突副本并提示用户；日历中已存在的事件不被自动覆盖。
- 设置：语言、默认目标日历、统一的系统日历提醒策略、明确时间点前后 1 小时规则开关、是否允许电脑服务器解析、数据导出与删除、退出登录。
- 支持在账号中心查看已登录设备并执行远程退出；服务端应提供账号注销和数据删除能力。

## 5. 关键交互流程

### 流程 A：图片包含明确时间

首页 → 上传图片 → OCR 处理中 → 展示/编辑原文 → 自动解析 → 结果确认页 → 创建提醒 → 成功页。

### 流程 B：粘贴文字且无时间

首页 → 粘贴文字 → 自动解析 → 识别到事项但缺时间 → 默认生成全天事件（用户可修改日期或改为具体时间）→ 结果确认页 → 创建提醒 → 成功页。

### 流程 C：系统分享

微信/浏览器等应用 → 系统分享面板 → 选择日历提醒助手 → 接收文字或图片 → OCR/解析 → 结果确认页 → 创建提醒。

### 流程 D：多个事项

解析结果页展示事项列表，用户可勾选一项或多项；每项可单独编辑。批量创建前显示将写入的事件数量。

### 状态与异常

- 加载：显示当前阶段（上传、OCR、解析、写入），支持取消。
- 解析失败：允许重试、编辑原文后重试或手动创建。
- 低置信度：对不确定字段显示提示，不阻塞用户修改。
- 权限拒绝：解释权限用途，提供“去系统设置”入口。

## 6. 大模型与技术方案（本地电脑服务器优先）

### 6.1 推荐 MVP 方案

1. **前端框架**：uni-app（Vue 3 + TypeScript），通过 UTS/uni 原生插件调用 Android OCR、系统分享和 Calendar Provider。
2. **后端语言与框架**：Python 3.11 + FastAPI，运行在用户电脑上；SQLite 保存账号、文字、草稿和同步数据。
3. **OCR**：Google ML Kit Text Recognition（手机端侧，免费；按语言包支持中文和拉丁文字）。
4. **端侧结构化解析模型**：Qwen2.5-1.5B-Instruct（Apache-2.0，可通过 llama.cpp/MediaPipe 等方式量化运行）。
5. **电脑服务器解析模型**：由用户电脑运行量化后的 Qwen 模型服务，手机通过局域网 API 调用；电脑不可用时回退到端侧模型或手动创建。
6. **解析方式**：将 OCR/粘贴文本和设备当前时间、时区传入电脑服务，要求仅输出 JSON；客户端使用 JSON Schema 校验，失败则重试或转人工编辑。

选择理由：端侧模型保证离线可用，电脑服务器模型用于复杂文本兜底和统一账号同步；数据保存在用户自己的电脑上，不依赖第三方云 API 或持续调用费用。

### 6.2 电脑服务器解析模式

- 电脑服务器解析为可选能力，首次连接前显示数据处理说明并获得同意；用户可在设置中关闭。
- 手机与电脑首次配对使用一次性验证码或二维码，后续使用短期访问令牌；仅允许已配对设备访问局域网 API。
- 端侧解析失败、低置信度或用户主动选择时才调用电脑服务；电脑不可用时提供明确的离线回退路径。
- 当前阶段不接入第三方云模型；未来如增加公网云服务，必须另行取得同意并更新隐私政策。

### 6.3 模型输出约束

```json
{
  "events": [
    {
      "title": "提交季度报销单",
      "anchorTime": "2026-09-01T14:00:00+08:00",
      "startTime": "2026-09-01T09:00:00+08:00",
      "endTime": null,
      "dueTime": "2026-09-01T18:00:00+08:00",
      "allDay": false,
      "location": null,
      "notes": "携带发票原件",
      "recurrence": null,
      "confidence": 0.92,
      "missingFields": []
    }
  ],
  "warnings": []
}
```

客户端必须校验时间格式、时区、结束时间晚于开始时间、标题非空；任何校验失败都不能直接写入日历。

## 7. 数据、隐私与安全

- 默认端侧处理；图片和原文仅在用户主动保存历史时落盘。登录后同步结构化结果，原始图片默认不同步。
- 手机本地数据使用 Android Keystore 管理密钥进行加密；电脑服务器使用系统密钥库或独立密钥文件加密数据库、访问令牌和备份。
- 不收集通讯录、短信、位置等非必要权限。
- 图片处理完成后可自动删除临时文件；设置中提供立即清除功能。
- 电脑服务器解析需单独同意，隐私说明局域网传输字段、长期保存范围和已配对设备访问方式；当前不向第三方云服务传输内容。
- 日历写入遵循最小权限原则，仅申请读取/写入日历所需权限。

## 8. 非功能需求

- Android 8.0（API 26）及以上；适配常见手机分辨率和深色模式。
- 端侧模型设备基线建议：优先支持内存不低于 4 GB 的设备；量化模型安装包目标不超过 1.5 GB。低于该基线时优先使用电脑服务器解析或提供手动创建。
- 端侧 OCR 首次结果目标：普通图片 3 秒内；文本解析目标：5 秒内（中端设备）。
- 无网络或电脑不可用时，端侧 OCR 和模型解析仍可用；电脑服务器恢复连接后，同步内容从本地队列重试。
- 崩溃率、解析失败率和重复创建率应可统计（仅收集匿名技术指标，默认不上传内容）。
- 所有关键操作可用 TalkBack；字号放大时布局不重叠。

## 9. MVP 验收标准

1. 用户可切换中文和 English 界面，并从相册选择一张包含中文或英文事项的图片完成 OCR、编辑和解析。
2. 用户可粘贴至少 500 字文本并得到结构化事项。
3. 对明确日期时间的单事项，确认后能在手机系统自带日历中创建以识别时间点前后各 1 小时为区间的事件。
4. 对无时间事项，应用默认生成选定日期的全天事件，且事件日期包含完整年、月、日。
5. 用户可通过 Android 系统分享面板发送文字或图片并完成同等解析流程。
6. 用户登录后可在另一台设备看到同步的草稿、历史和偏好设置；离线修改可在联网后合并。
7. 对多个事项，用户可选择性创建且每个事件内容正确。
8. 权限拒绝、解析失败、日历写入失败均有可理解的恢复路径。
9. 电脑服务器解析默认需用户同意，用户可关闭；应用内能查看、导出并删除手机本地及电脑服务器历史。

## 10. 评估指标（试用阶段）

- 事项解析准确率：标题 ≥ 90%，明确时间 ≥ 95%。
- 首次创建成功率 ≥ 85%。
- 从输入到确认的中位操作时长 ≤ 30 秒。
- 重复事件率 < 1%。
- 用户主动修改解析结果的比例，用于定位模型问题。

## 11. 版本规划

### V1（MVP）

单图/文本/系统分享输入、端侧 OCR、中英文切换、单/多事项解析、明确时间点前后 1 小时区间、无时间默认全天、系统自带日历写入、邮箱账号登录、跨设备同步、基础历史和设置、可选电脑服务器解析。

### V1.1

相机拍摄优化、批量图片、更多重复规则、解析模板、账号安全增强、匿名质量反馈。

### V2（待验证）

通知栏快捷入口、自然语言修改已有事件、多人共享日历、更多语言和平台。

## 12. 待确认问题

1. 目标设备最低内存和可接受的端侧模型安装包大小是多少？（建议 MVP 目标：4 GB 内存、模型量化包不超过 1.5 GB。）
2. 全天事件统一采用当天 09:00 作为默认系统日历提醒；用户可关闭或调整该提醒。明确时间点场景以 `T-1h ~ T+1h` 作为系统日历事件区间，提醒设置可由用户关闭或调整。
