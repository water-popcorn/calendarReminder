# 日历提醒助手概要设计文档

**文档版本**：V1.1  
**依据文档**：产品需求文档-日历提醒助手.md  
**适用范围**：V1（MVP）  
**编写日期**：2026-08-29

## 1. 设计目标与原则

### 1.1 设计目标

- 手机端使用 uni-app 完成图片/文字输入、OCR、结果确认和手机系统日历写入。
- 电脑端作为局域网服务器，负责邮箱账号、长期数据保存、跨设备同步和可选的大模型解析。
- 端侧优先、服务器可选：电脑不可用时核心解析流程仍可运行。
- 所有模型输出必须经过结构化校验和用户确认，禁止静默创建日历事件。

### 1.2 设计原则

1. **本地优先**：默认使用手机端 OCR 与轻量模型；原始图片不上传、不长期保存。
2. **边界清晰**：手机负责相机、相册、系统分享和 Calendar Provider，电脑负责业务数据和模型服务。
3. **可恢复**：网络中断、电脑离线、权限拒绝或写入失败时保留草稿并支持重试。
4. **最小权限**：仅申请相机、相册、日历和网络相关权限；不读取通讯录、短信和位置。
5. **可迁移**：服务器数据可加密备份并迁移到另一台电脑。

## 2. 总体架构

### 2.1 逻辑架构

~~~text
┌──────────────────────── uni-app 手机端 ───────────────────────┐
│ Vue 页面 │ 输入/分享 │ OCR 插件 │ 端侧解析插件 │ 日历/分享原生插件 │
│ 本地草稿/队列 │ 账号会话 │ 同步客户端 │ 设置与历史 UI │
└───────────────┬───────────────────────────────┬───────────────┘
                │局域网 HTTPS/受控端口           │Android Calendar Provider
                ▼                               ▼
┌──────────────────────── 电脑本地服务器 ───────────────────────┐
│ API 网关 │ 认证服务 │ 同步服务 │ 解析编排器 │ Qwen 模型服务 │
│ SQLite 数据库 │ 备份/恢复 │ 设备配对 │ 日志与健康检查 │
└───────────────────────────────────────────────────────────────┘
~~~

### 2.2 部署形态

- 电脑运行一个可开机启动的服务器进程，默认绑定局域网地址，不开放公网。
- 后端确定使用 Python 3.11 + FastAPI，SQLite 保存数据，模型通过 llama.cpp/Ollama 兼容接口运行。
- 手机与电脑首次通过二维码/一次性验证码配对，之后使用访问令牌。
- 手机和电脑不在同一局域网时，V1 不保证同步；手机仍可端侧运行，联网后再同步。
- 当前 MVP 不引入域名或公网依赖。

## 3. uni-app 客户端设计

### 3.1 模块划分

| 模块 | 主要职责 |
| --- | --- |
| pages/components | 首页、输入、OCR 编辑、解析结果、确认、历史、设置、登录 |
| input/share | 相册、相机、剪贴板和 Android Sharesheet |
| native-ocr | 通过 UTS/uni 原生插件调用 ML Kit |
| parser | 端侧 Qwen 插件、提示词、JSON 校验和置信度 |
| calendar-plugin | 通过 UTS/原生插件调用 CalendarContract |
| sync | 局域网请求、配对、会话、同步和离线队列 |
| storage | uni.storage、SQLite/原生加密存储和草稿 |
| common | Vue I18n、多语言、时间、权限、错误码和日志 |

### 3.2 推荐技术栈

- uni-app（Vue 3 + TypeScript）与 Vite；状态管理使用 Pinia，国际化使用 Vue I18n。
- UI 使用 uni-ui 或项目自定义组件；网络使用 uni.request。
- 图片/相册使用 uni.chooseImage，系统分享使用 UTS/Android 原生扩展接收 ACTION_SEND。
- OCR、CalendarContract、Keystore 和端侧模型通过 UTS 插件或 uni 原生插件封装，页面层不直接依赖 Android API。
- 本地草稿使用 uni.storage；需要事务和加密时使用 SQLite 原生插件 + Android Keystore。
- 服务器使用 Python 3.11、FastAPI、Pydantic、SQLAlchemy/SQLModel、SQLite 和 Alembic；模型运行使用 llama.cpp 或 Ollama。

### 3.3 输入与分享处理

- ACTION_SEND 接收 text/plain、image/* 和 content URI；先复制到应用临时目录，再交给统一处理管线。
- 图片仅在本地临时处理，OCR 完成后默认删除临时文件。
- 所有输入生成唯一 requestId，用于取消、重试和防重复创建。

### 3.4 解析与确认

1. 输入预处理：清理空白、规范化日期分隔符、识别语言。
2. 优先端侧解析；用户开启电脑解析或端侧置信度低时调用服务器。
3. 使用固定 JSON Schema 校验 title、anchorTime、startTime、endTime、allDay 等字段。
4. 日期必须由用户确认完整的年、月、日；缺失时阻止创建并打开日期选择器。
5. 明确时间点 T：默认 startTime=T-1h、endTime=T+1h；无明确时间：默认 allDay=true。
6. 解析结果进入确认页，用户修改后才允许写入系统日历。

## 4. 手机系统日历设计

### 4.1 写入边界

- 仅 uni-app Android 原生插件调用 CalendarContract；电脑服务器不直接写手机日历。
- 首次写入前申请 READ_CALENDAR、WRITE_CALENDAR，拒绝时保留草稿。
- 目标日历由用户选择，保存 calendarId；日历被删除或失效时要求重新选择。

### 4.2 事件映射

保存 eventMapping：requestId、设备 ID、calendarId、系统事件 _ID、最后同步时间。

- 创建前检查本地映射，避免重复点击产生重复事件。
- 编辑草稿后若已有系统事件，仅更新由本应用创建且映射有效的事件。
- 不修改用户手动创建的同名事件，也不自动删除已有系统日历事件。

### 4.3 提醒规则

- 明确时间点：事件区间默认为 T-1h ~ T+1h，系统日历默认在事件开始时提醒；用户可关闭或调整。
- 无时间点：创建全天事件，默认当天 09:00 提醒；用户可关闭或调整。
- 不额外创建应用自有通知，最终提醒行为以手机自带日历对 Calendar Provider 的支持为准。

## 5. 电脑服务器设计

### 5.1 服务模块

| 服务 | 职责 |
| --- | --- |
| API 网关 | 请求鉴权、参数校验、限流、统一错误响应 |
| 认证服务 | 邮箱注册、验证码、密码哈希、登录、刷新令牌、注销 |
| 设备服务 | 二维码/验证码配对、设备列表、远程退出 |
| 解析服务 | Qwen 模型加载、提示词、JSON Schema 校验和重试 |
| 同步服务 | 草稿/历史/设置的增量同步、版本号和冲突处理 |
| 数据服务 | SQLite 读写、软删除、导出、备份与恢复 |
| 运维服务 | 健康检查、日志、模型状态、磁盘空间告警 |

### 5.2 账号与安全

- 注册：邮箱 + 密码 + 邮箱验证码；邮箱作为唯一账号标识。
- 密码使用 Argon2id（不可逆哈希）保存；登录、验证码和重置密码接口限流。
- 会话采用短期 Access Token + 可撤销 Refresh Token；退出登录时撤销令牌。
- 设备配对令牌只显示一次并设置有效期；服务器仅接受已配对设备的同步请求。
- 局域网仍按不可信网络处理，API 需要令牌、时间戳和请求签名/随机数防重放。

### 5.3 数据保存与备份

- 数据库长期保存账号、文字原文、结构化结果、草稿、事件映射、设备和审计记录。
- 原始图片不上传服务器；服务器只接收 OCR 文本或用户粘贴文字。
- 提供手动加密备份、定期备份、恢复前校验和导出 JSON/CSV。
- 账号注销后删除账号关联数据；删除操作写入审计记录但不保留可恢复的业务内容。

## 6. 数据模型（概要）

### 6.1 users

id、email、password_hash、email_verified、locale、created_at、updated_at、deleted_at

### 6.2 devices

id、user_id、name、platform、pairing_state、token_hash、last_seen_at、created_at、revoked_at

### 6.3 requests

id/request_id、user_id、source_type（image/text/share）、source_text、language、parse_mode、status、model_version、created_at、updated_at、deleted_at

### 6.4 events

id、request_id、title、anchor_time、start_time、end_time、due_time、all_day、timezone、location、notes、recurrence、confidence、missing_fields、user_edited

### 6.5 calendar_mappings

id、event_id、device_id、calendar_id、system_event_id、write_status、last_written_at

### 6.6 sync_changes

id、user_id、entity_type、entity_id、operation、version、updated_at、device_id

## 7. 接口设计（概要）

接口前缀：/api/v1。请求和响应使用 UTF-8 JSON；时间统一 ISO 8601，并携带时区偏移。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /auth/register | 邮箱注册并发送验证码 |
| POST | /auth/verify-email | 校验邮箱验证码 |
| POST | /auth/login | 邮箱密码登录 |
| POST | /auth/refresh | 刷新访问令牌 |
| POST | /auth/logout | 撤销当前会话 |
| POST | /auth/reset-password | 重置密码 |
| POST | /devices/pair | 设备配对 |
| GET | /devices | 查询已登录设备 |
| DELETE | /devices/{id} | 撤销设备 |
| POST | /parse | 使用电脑模型解析文字 |
| GET | /requests/{id} | 查询请求和解析结果 |
| POST | /sync/pull | 拉取指定版本后的变更 |
| POST | /sync/push | 提交本地变更，返回冲突信息 |
| GET | /export | 导出账号数据 |
| POST | /backup | 创建加密备份 |
| DELETE | /account | 注销并删除账号数据 |
| GET | /health | 服务与模型健康检查 |

### 7.1 /parse 请求示例

~~~json
{
  "requestId": "req_01H...",
  "text": "9月1日下午2点提交报销单",
  "locale": "zh-CN",
  "now": "2026-08-29T10:00:00+08:00",
  "timezone": "Asia/Shanghai",
  "schemaVersion": "1.0"
}
~~~

响应为 PRD 中定义的 events[] + warnings[] 结构，并附带 modelVersion、serverTime 和 needsConfirmation。

## 8. 同步设计

### 8.1 同步内容

- 同步文字原文、解析结果、草稿状态、用户编辑字段、偏好设置和系统事件映射。
- 不同步原始图片；图片仅存在手机临时目录或用户明确保存的本地文件。

### 8.2 同步流程

1. 手机登录并完成设备配对，获取 deviceId 和令牌。
2. 手机提交本地 sync_changes 游标，服务器返回增量变更。
3. 手机上传离线变更，服务器按实体版本校验并返回成功或冲突。
4. 冲突默认保留服务器版本和冲突副本，用户在历史/草稿页选择合并。
5. uni-app 应用生命周期配合原生后台任务插件，在网络可用时重试同步；前台打开应用时优先执行待同步队列。

## 9. 模型与解析编排

- 默认端侧 OCR；服务器只接收文本，不接收原始图片。
- 端侧模型：Qwen2.5-1.5B-Instruct 量化版本；设备内存不足时不加载模型。
- 电脑模型：同系列较大或量化模型，具体尺寸根据电脑 CPU/GPU 与内存测试确定。
- 提示词要求：输出严格 JSON、不得补全未确认日期、明确区分 anchorTime 与事件区间。
- 服务端最多自动重试 1 次；JSON 仍不合法则返回可编辑原文和错误码，不创建事件。
- 模型版本写入 requests.model_version，便于回溯和重新解析。

## 10. 异常与恢复

| 场景 | 处理 |
| --- | --- |
| OCR 失败 | 提示重拍/重选，允许编辑或粘贴文字 |
| 电脑不可达 | 回退端侧解析；同步进入本地队列 |
| 模型超时 | 取消请求，保留原文和草稿，支持重试 |
| 日期不完整 | 阻止创建，打开日期选择器 |
| 日历权限拒绝 | 说明用途，提供系统设置入口 |
| 日历写入失败 | 保留草稿和错误信息，支持重试 |
| 重复创建 | 依据 requestId 与 calendar_mappings 幂等检查 |
| 同步冲突 | 保留冲突副本，要求用户选择 |
| 磁盘空间不足 | 停止备份/解析写入，提示清理空间 |

## 11. 安全与隐私

- 局域网 API 默认仅绑定私有网卡；提供允许的网段配置和一键停止服务。
- 服务器数据库、备份和令牌使用本地加密；日志不得记录密码、令牌、完整原文或邮箱验证码。
- 图片处理后删除临时文件；服务端不保存原始图片。
- 账号注销、数据导出和删除操作需要重新验证密码或验证码。
- 提供隐私设置：端侧解析、电脑服务器解析、历史保存和设备管理。

## 12. 可观测性与运维

- /health 返回 API、数据库、模型、磁盘空间和版本状态。
- 日志分为错误、审计和性能三类，默认只保存在电脑本地。
- 关键指标：OCR 耗时、解析耗时、解析失败率、同步失败率、日历写入成功率、重复创建拦截数。
- 支持导出脱敏诊断包，用户主动选择后才分享给开发者。

## 13. 测试策略

- 单元测试：时间解析、T-1h/T+1h 区间、全天事件、日期完整性、JSON Schema、幂等键。
- 集成测试：登录/验证码、设备配对、同步冲突、数据库备份恢复、局域网断连重试。
- Android 真机测试：Calendar Provider、AOSP/厂商日历、权限拒绝、系统分享、深色模式和 TalkBack。
- 模型评测集：中文/英文、绝对/相对时间、多事项、无时间、歧义日期、低质量 OCR 文本。
- 安全测试：令牌撤销、重放请求、暴力登录、越权访问、备份泄露和账号注销。

## 14. 开发与发布顺序

1. 搭建 uni-app 工程、电脑服务器工程和局域网健康检查。
2. 完成图片/文字/系统分享输入与端侧 OCR。
3. 完成解析结果模型、确认页和系统日历写入。
4. 完成邮箱注册登录、设备配对和本地数据库。
5. 完成同步、冲突处理、长期保存、备份恢复和账号注销。
6. 接入电脑 Qwen 模型，完成端侧/服务器解析切换。
7. 完成中英文、多设备、异常、安全和性能测试后发布试用包。

## 15. 关键技术决策记录

| 决策 | 结论 | 原因 |
| --- | --- | --- |
| 服务器位置 | 用户自己的电脑 | 无需云服务费用，数据掌握在用户手中 |
| 图片处理 | 手机端 OCR，原图不上传 | 降低隐私风险和带宽消耗 |
| 系统提醒 | 使用 Android 自带日历 | 满足“手机自带日历”要求，避免重复通知 |
| 明确时间 | 默认事件区间 T-1h ~ T+1h | 覆盖时间点前后各一小时 |
| 无时间 | 默认全天事件，默认 09:00 提醒 | 统一规则，用户可关闭/调整 |
| 端侧模型 | Qwen2.5-1.5B 量化 | 兼顾中文能力、离线可用和安装包大小 |
